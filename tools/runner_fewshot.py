def run_net(*args, **kwargs):
    raise RuntimeError(
        "Few-shot training code has been removed from the inference-only release."
    )


def test_net(*args, **kwargs):
    raise RuntimeError(
        "Few-shot utilities are not available in the inference-only release."
    )
